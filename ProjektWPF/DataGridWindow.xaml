<Window WindowStartupLocation="CenterScreen" x:Class="ProjektWPF.DataGridWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProjektWPF"
        mc:Ignorable="d"
        Title="Felhasználók" Height="450" Width="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#004d00" Offset="0.0"/>
                <GradientStop Color="#3CB371" Offset="1.0"/>
            </LinearGradientBrush>
        </Grid.Background>

        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
            <Label Content="Város:" VerticalAlignment="Center" Margin="10" Foreground="White"/>
            <ComboBox Name="FilterComboBox"
                      HorizontalAlignment="Left" 
                      VerticalAlignment="Center" 
                      Width="150" 
                      Margin="10"
                      SelectionChanged="FilterComboBox_SelectionChanged"/>
            <Button Name="SaveButton" Content="Mentés" HorizontalAlignment="Left" VerticalAlignment="Top"
                    Width="100" Height="30" Margin="10" Grid.Row="1" Click="SaveButton_Click" 
                    Background="#3CB371" Foreground="White" FontSize="14" FontWeight="Bold"
                    BorderThickness="0" Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" 
                                            CornerRadius="20" 
                                            BorderBrush="Transparent">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Setter Property="Background" Value="#3CB371" />
                        <Setter Property="Foreground" Value="White" />
                        <Setter Property="FontSize" Value="14" />
                        <Setter Property="FontWeight" Value="Bold" />
                        <Setter Property="Cursor" Value="Hand" />
                        <Setter Property="Width" Value="100" />
                        <Setter Property="Height" Value="30" />
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#2E8B57" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#228B22" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>

        <DataGrid Name="DataGridUsers"
                  Grid.Row="1"
                  AutoGenerateColumns="True"
                  HorizontalAlignment="Stretch" 
                  VerticalAlignment="Stretch"
                  CanUserAddRows="False"
                  IsReadOnly="False"
                  CellEditEnding="DataGridUsers_CellEditEnding"
                  AutoGeneratingColumn="DataGridUsers_AutoGeneratingColumn"
                  Background="#004d00"
                  BorderBrush="Transparent">

            <DataGrid.Columns>
                <DataGridCheckBoxColumn Header="Elmaradva" Binding="{Binding FizetettE_havi}" />
                <DataGridTextColumn Header="Fizetési Elmaradás" Binding="{Binding Fizetesi_elmaradas}" />
                <DataGridTextColumn Header="Név" Binding="{Binding FullName}" />
                <DataGridTextColumn Header="Szövetkezet" Binding="{Binding LakasSzovNev}" />
                <DataGridTextColumn Header="Születési Idő" Binding="{Binding DateOfBirthFormatted}" />
                <DataGridTextColumn Header="Telefonszám" Binding="{Binding PhoneNumber}" />
            </DataGrid.Columns>

            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="Background" Value="#3CB371"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="Height" Value="30"/>

                    <Style.Triggers>
                        <DataTrigger Binding="{Binding FizetettE_havi}" Value="True">
                            <Setter Property="Background" Value="Crimson"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>

            <DataGrid.CellStyle>
                <Style TargetType="DataGridCell">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderBrush" Value="Transparent"/>
                    <Setter Property="Foreground" Value="White"/>
                </Style>
            </DataGrid.CellStyle>

            <DataGrid.SelectionUnit>
                <DataGridSelectionUnit>Cell</DataGridSelectionUnit>
            </DataGrid.SelectionUnit>

        </DataGrid>
    </Grid>
</Window>
